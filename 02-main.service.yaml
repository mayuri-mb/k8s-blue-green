apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: LoadBalancer
  clusterIP: None
  selector:
    project: roboshop
    tier: frontend
    component: frontend  
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80             
